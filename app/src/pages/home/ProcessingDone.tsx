import { FC } from "react";

const ProcessingDone: FC = () => {
  return (
    <main className="processing-done">
      <div>
        <span className="loader-label">Your video is ready !</span>
      </div>
    </main>
  );
};

export default ProcessingDone;